import Invoices from "components/Invoices";
import Layout from "components/layout";
import React from "react";

const faturalar = () => {
  return (
    <Layout pageTitle="Faturalar">
      <Invoices />
    </Layout>
  );
};

export default faturalar;
